<rule name="blog to knowledge-base">
    <match url="^(.*)/blog/(.*)" />
    <action type="Rewrite" url="{R:1}/knowledge-base/{R:2}" />
</rule>
<rule name="main-concepts to knowledge-base">
    <match url="^(.*)/main-concepts/(.*)" />
    <action type="Rewrite" url="{R:1}/knowledge-base/{R:2}" />
</rule>
<rule name="features to knowledge-base">
    <match url="^(.*)/features/(.*)" />
    <action type="Rewrite" url="{R:1}/knowledge-base/{R:2}" />
</rule>
<rule name="features to knowledge-base">
    <match url="^(.*)/guides/(.*)" />
    <action type="Rewrite" url="{R:1}/knowledge-base/{R:2}" />
</rule>